@page "/cozinha"

<PageTitle>Cozinha</PageTitle>

<MudContainer MaxWidth="MaxWidth.ExtraExtraLarge" Class="mt-4">
    <MudPaper Elevation="0" Class="pa-6 mb-6" Style="border-radius: 12px; border-left: 4px solid var(--mud-palette-primary);">
        <div class="d-flex align-center justify-space-between">
            <div class="d-flex align-center">
                <MudAvatar Color="Color.Primary" Variant="Variant.Filled" Size="Size.Large" Style="background: rgba(var(--mud-palette-primary-rgb), 0.1);">
                    <MudIcon Icon="@Icons.Material.Filled.Restaurant" Color="Color.Primary" Size="Size.Medium" />
                </MudAvatar>
                <div class="ml-4">
                    <MudText Typo="Typo.h4" Style="font-weight: 700;">
                        Cozinha
                    </MudText>
                    <MudText Typo="Typo.body1" Style="opacity: 0.7; margin-top: 4px;">
                        Gerencie o fluxo de preparação dos pedidos
                    </MudText>
                </div>
            </div>
            <div class="d-flex gap-2 align-center">
                <MudIconButton Icon="@Icons.Material.Filled.Refresh" 
                              Color="Color.Default" 
                              Variant="Variant.Outlined"
                              Size="Size.Medium"
                              Style="border-radius: 8px;" />
                <MudChip T="string" Size="Size.Medium" Color="Color.Success" Variant="Variant.Filled">
                    <MudIcon Icon="@Icons.Material.Filled.Circle" Size="Size.Small" Class="mr-1" />
                    Operando
                </MudChip>
            </div>
        </div>
    </MudPaper>
    <KitchenBoard />
</MudContainer>

